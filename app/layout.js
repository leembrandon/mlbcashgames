import "./globals.css";

export const metadata = {
  title: "MLB Cash Analyzer",
  description: "MLB DFS cash game analyzer powered by Daily Fantasy Fuel projections",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link
          href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700;800&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
